#include "main.h"

/**
 * _abs -  absolute value of a number
 *
 * @num: integer number
 *
 * Return: return -num or num
 */

int _abs(int num)
{
	if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
}
