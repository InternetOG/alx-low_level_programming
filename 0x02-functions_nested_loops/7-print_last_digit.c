#include "main.h"
/**
 * printf_last_digit - prints the last number
 *
 * @num: integer variable
 *
 * Return: last_digit
 */

int print_last_digit(int num)
{
	int last_digit = num % 10;
	return (last_digit);

}
